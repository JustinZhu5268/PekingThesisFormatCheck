# Cursor 启动指令

## 你的任务

你即将开发一款北大光华 EMBA 论文格式自动化审查工具。所有需求、架构、规则、代码规范均已写在 PRD 中，你必须严格遵循。

## 第一步：阅读以下两个文件

1. **PRD（产品需求文档）**：`EMBA论文审查工具_PRD终极版.md`
   - 通读全文，重点理解：
     - 第一部分「七条红线要求」— 这是不可违反的硬约束
     - 第三部分「七种 check_type 模板」— 这是规则引擎的核心
     - 附录A「MD 清单自动解析规范」— 这是 Step 1 的实现蓝图

2. **MD 清单（规则源文件）**：`EMBA论文格式排版要求清单_补充完善版.md`
   - 通读全文，理解其表格结构（5列：要素 | 格式要求 | 紧急程度 | 可编程性 | 检测方法说明）
   - 注意：只有「可编程性」列为 ✅ 或 ⚠️ 的行才纳入规则库

## 第二步：按 PRD 第十三部分的 Step 0 到 Step 10 顺序执行

**严格按顺序，每完成一个 Step 必须停下来告诉我：**
- 本步完成了什么
- 交付物清单
- 是否通过了验收标准
- 遇到的问题或需要我确认的事项

**不要一次性生成所有代码。** 一步一停，等我确认后再继续下一步。

## 当前请执行：Step 0（环境准备与技术预研）

按 PRD 的指示：
1. 创建项目目录结构
2. 创建 `requirements.txt`
3. 创建 `config.py`（所有常量：EMU换算系数、字号映射、默认路径等）
4. 创建 `utils.py`（单位换算函数、安全XML读取封装、中文字体读取）
5. 列出 python-docx 无法检测的规则，标记为 SKIP_LIST

完成后告诉我交付物清单和验收结果。
